[tool.poetry]
name = "heavy-duty"
version = "0.1.0"
description = ""
authors = ["Tomas Björklund <snorlena@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
tabulate = "^0.9.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
